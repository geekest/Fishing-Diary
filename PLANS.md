# PLANS.md

本文件定义本仓库任务级 ExecPlan 的最小结构。Agent 在执行中大型任务前，必须先按全局 `~/.codex/PLANS.md` 判断是否需要创建计划；需要创建时，将计划保存到 `.codex/plans/<任务名>.md`，并至少包含以下章节。

如果全局 `~/.codex/PLANS.md` 与本文件存在差异，以全局规范为执行要求，本文件只作为仓库内的最小模板补充。

## Goal

目标。

## Current State

当前状态。

## Constraints

约束。

## Plan

执行步骤。

## Risks

风险。

## Validation

验证方式。
