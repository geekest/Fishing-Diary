# Fishing Diary

钓鱼日记是一款面向钓友的 iOS 应用原型，用来记录每一次出钓、整理渔获照片，并快速生成适合社交平台发布的渔获分享卡。

应用当前聚焦在一个完整的 MVP 流程：选择渔获照片、裁剪/抠图、填写鱼种和尺寸、带入环境数据、存入本地日记，并从记录生成分享图片。

## 图标方向

当前推荐方向围绕“钓鱼记录 + 分享图”展开：用记录页、分享卡片和鱼钩线索表达产品核心能力。完整 1024×1024 PNG 文件位于 [`docs/app-icon-concepts`](docs/app-icon-concepts)。

![App 图标方向](docs/app-icon-concepts/icon-concept-v2-record-share.png)

## 功能亮点

- **渔获日记**：按月份展示出钓记录，支持按鱼种筛选。
- **多鱼记录**：一次出钓可以记录多尾鱼，分别保存鱼种、体长、重量、钓法和备注。
- **照片处理**：支持从相册选择最多 9 张图片，并通过裁剪流程生成渔获展示图。
- **环境数据**：记录钓点、天气、风速、气压、紫外线、潮汐、月相等字段；当前使用本地预设 + 手动编辑，不依赖 WeatherKit。
- **分享卡片**：将渔获、地点和环境数据渲染为 3:4、1:1、9:16 分享图，适合小红书、朋友圈和故事场景发布。
- **本地优先**：当前阶段不依赖 Apple 付费开发者账号，优先完善本地记录、导出、分享和手动兜底体验。

## 应用流程

1. 首次打开完成引导页。
2. 从日记首页点击新增，选择渔获照片。
3. 逐张裁剪/抠图，生成每尾鱼的图片素材。
4. 填写鱼种、体长、重量、钓法和备注。
5. 选择要写入记录的环境数据字段。
6. 保存到本地日记，并可继续生成分享卡片。

## 技术栈

- Swift 5.9
- SwiftUI
- SwiftData
- XcodeGen
- TOCropViewController
- iOS 18.0+

## 项目结构

```text
FishingDiary/
├── App/                 # App 入口与根视图
├── Common/              # 主题、通用组件和卡片视图
├── Models/              # SwiftData 模型与记录流程状态
├── Resources/           # Info.plist、Assets、Entitlements
├── Services/            # 天气、购买、图片渲染服务
└── Views/               # 引导、记录、日记、分享、我的页面
```

## 本地运行

项目使用 XcodeGen 维护工程文件。如果你修改了 `project.yml`，请重新生成 Xcode 工程：

```bash
xcodegen generate
```

然后打开工程并运行：

```bash
open FishingDiary.xcodeproj
```

默认配置：

- Bundle ID：`com.placeholder.FishingDiary`
- App 名称：`钓鱼日记`
- 目标设备：iPhone
- 最低系统：iOS 18.0

> 当前阶段不需要 Apple 付费开发者账号即可在模拟器中验证核心流程。上架前再在 `project.yml` 中填入真实 Team ID，并单独评估 WeatherKit / CloudKit entitlements。

## 当前状态

这是一个可运行的 MVP 原型，核心记录链路和分享卡片渲染已经搭好。当前已经完成或可验证的能力包括：

- 本地 SwiftData 日记、鱼种筛选、日期筛选和记录编辑。
- 钓点定位与反地理编码；定位失败时仍可手动填写钓点。
- 多画幅分享图、相册保存反馈和分享卡模板扩展。
- Mock 购买状态；当前默认解锁模板与高清导出，后续接入真实 StoreKit 前不阻塞体验验收。

以下能力仍处于后续规划阶段：

- 更完整的自动抠图能力。
- 真实天气服务、真实购买和云同步；这些能力需要 Apple Developer Program 或上架前配置，暂不作为近期 MVP 阻塞项。

## 路线图

- [x] 接入真实定位和反地理编码
- [x] 增加更多分享卡模板和多画幅导出
- [x] 支持分享图高清导出和相册保存结果反馈
- [ ] 增加本地日志导出，先用 CSV / JSON 解决备份和迁移
- [ ] 完善环境数据的手动兜底与输入状态提示
- [ ] 增加离线样例数据或空状态引导，方便无账号环境演示
- [ ] 补充单元测试和 UI 测试

### 暂缓到付费开发者账号或上架准备阶段

- [ ] 接入 WeatherKit，替换本地预设天气服务。
- [ ] 接入 StoreKit 2，支持真实买断、订阅和恢复购买。
- [ ] 增加 CloudKit / iCloud 同步与云端备份。

## 许可证

本项目基于 [MIT License](LICENSE) 开源。
