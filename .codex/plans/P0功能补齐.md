# P0 功能补齐执行计划

## 需求理解
- 保留已做的日记日期筛选，不重复实现功能点 1。
- 补齐 P0 剩余功能：分享多画幅、相册保存反馈、设置页 MVP。
- 每个功能点单独提交一个 commit。
- 最后更新 PR 标题和描述为中文最新内容。

## 实现方案
1. 分享多画幅：打通 1:1、9:16 选择到预览和导出，保持使用现有 MinimalCardView，不新增模板。
2. 相册保存反馈：为保存相册增加回调，区分成功和失败提示，并保留分享面板。
3. 设置页 MVP：新增设置页，支持昵称、默认重量单位、协议与隐私占位说明入口。

## 风险与取舍
- 不接入真实 StoreKit、WeatherKit 或外部依赖。
- 不修改 SwiftData 模型。
- 设置页偏好使用 AppStorage，避免数据迁移。

## 验证方式
- 每个功能点完成后检查 diff 并提交。
- 最后运行 `git diff --check`。
- 如环境支持则运行 iOS 构建；若缺少 `xcodebuild`，记录环境限制。

## 执行记录
- 已完成分享多画幅：1:1、3:4、9:16 可进入预览并按对应尺寸导出。
- 已完成相册保存反馈：保存成功和失败会显示不同提示，保存后仍可打开系统分享面板。
- 已完成设置页 MVP：支持昵称、默认重量单位、用户协议和隐私政策占位页；新建渔获默认使用偏好单位。

## 验证结果
- 已按功能点分别提交 commit。
- 待执行最终 diff 检查和环境构建检查。

## 结果复盘
- 未修改数据模型、权限、付费、WeatherKit 或生产配置。
- 新增设置偏好使用 `AppStorage`，避免引入迁移风险。
