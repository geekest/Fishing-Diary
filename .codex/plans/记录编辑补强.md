# 记录编辑补强执行计划

## 需求理解
- 在新分支补齐第六条：已保存记录的编辑体验一致性。
- 优先覆盖鱼种、体长、重量、钓法、备注、地点与天气字段。
- 保持不改 SwiftData 模型，不新增依赖，不做图片重选/重抠图这类风险更高的流程改造。

## 实现方案
1. 阅读详情页现有编辑入口和保存逻辑，复用已有 UI 风格。
2. 补齐编辑表单字段：单尾鱼字段、Session 级备注/钓法、地点、天气快照。
3. 保存时直接更新 SwiftData 模型对象，并处理可选数值和空字符串。
4. 做最小验证：diff 检查；若环境支持则构建。

## 风险与取舍
- 图片重选/重抠图涉及相册、裁剪、Vision 与已有记录图片替换，范围较大，本次不做。
- 天气字段按当前模型已有字段编辑，不新增潮汐/月相枚举或真实天气服务。

## 执行记录
- 已补齐单尾鱼钓法编辑，并保存到 `FishCatch.fishingMethod`。
- 已允许鱼种、体长、重量、地点和备注清空，保存时统一去除前后空白。
- 已补齐环境水温和月相编辑，天气字段继续使用现有 `WeatherSnapshot`。

## 验证结果
- `git diff --check HEAD~1..HEAD` 通过。
- `rg -n "methodPicker|水温|月相|normalizedDouble|fishingMethod|记录编辑补强" FishingDiary/Views/Diary/SessionDetailView.swift .codex/plans/记录编辑补强.md` 通过。
- `xcodebuild -project FishingDiary.xcodeproj -scheme FishingDiary -destination 'generic/platform=iOS Simulator' build` 因当前容器缺少 `xcodebuild` 未能执行。

## 结果复盘
- 本次未做图片重选/重抠图改造；现有换图能力保持原逻辑。
- 未修改数据模型和依赖，降低迁移风险。
